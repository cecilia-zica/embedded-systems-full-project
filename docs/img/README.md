# Mídia da demonstração

Coloque aqui as imagens/GIFs referenciados pelo README principal. Nomes esperados:

| Arquivo | O que capturar |
|---------|----------------|
| `app-logs.png` | Tela **Logs** do app (Flutter web ou celular), com algumas leituras |
| `app-config.png` | Tela **Config** com o limiar de BPM e o switch de alerta |
| `esp32-demo.gif` | GIF curto do ESP32 medindo, com o **LED** piscando |
| `arquitetura.png` | (opcional) diagrama exportado, se quiser além do mermaid |

Dicas:
- Flutter web: rode `flutter run -d chrome` e tire prints do navegador.
- GIF do dispositivo: grave um vídeo curto no celular e converta (ex.: com
  `ffmpeg` ou um site de vídeo→GIF). Mantenha < 5 MB pro README carregar rápido.

Depois de adicionar os arquivos, **descomente** o bloco de imagens na seção
"Demonstração" do [`README.md`](../../README.md) da raiz.
